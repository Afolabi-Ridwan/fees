import FormTemplate from "../FormTemplate";


const SubscriptionForm = () => {

    return (
        <FormTemplate formType="Subscription Form" heading="Subscribe" />
    );
};

export default SubscriptionForm;
